#include "main.h"

/**
* _abs - print the absolute value of the number input
*
* @n : input (int)
*
* Return: absolute value of the input
*/
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-1 * n);
}
