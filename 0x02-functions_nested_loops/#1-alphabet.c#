#include "main.h"

/**
 * print_alphabet - print all letters of the alphabet in lowercase
 *

void print_alphabet(void)
{
int start;
int stop;

start = 'a';
stop = 'z';
while (start <= stop)
{
_putchar(start);
start++;
}
_putchar('\n');
return;
}
