#include "main.h"

/**
* mul - multiply two input integers
*
* @a: input integer to be multiplied (multiplicant)
* @b: input integer to be multiplied (multiplier)
*
* Return: the product of a and b
*/

int mul(int a, int b)
{
return (a * b);

}
